export type CvTableCellProps = {
  /** Specify whether the overflow menu (if it exists) should be shown always, or only on hover */
  overflowMenuOnHover?: boolean;

  /** The table size. */
  size?: string;
};
