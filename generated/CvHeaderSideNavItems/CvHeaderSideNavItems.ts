export type CvHeaderSideNavItemsProps = {
  /** Optionally specify if container will have a bottom divider to differentiate
between original sidenav items and header menu items. False by default. */
  hasDivider?: boolean;
};
