export type CvAccordionItemProps = {
  /** `true` if the accordion item should be disabled. */
  disabled?: boolean;

  /** `true` if the accordion item should be open. */
  open?: boolean;

  /** The title text. */
  title?: string;
};
