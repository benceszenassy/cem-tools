export type CvLoadingProps = {
  /** The assistive text for the spinner icon. */
  assistiveText?: string;

  /** Spinner type. */
  type?: string;

  /** `true` if overlay should be applied. */
  overlay?: boolean;

  /** `true` if spinner should stop. */
  inactive?: boolean;
};
