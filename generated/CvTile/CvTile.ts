export type CvTileProps = {
  /** The color scheme. */
  colorScheme?: string;

  /** Specify if the `Tile` component should be rendered with rounded corners.
Only valid when `slug` prop is present */
  hasRoundedCorners?: boolean;
};
