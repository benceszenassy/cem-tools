export type CvStructuredListHeaderRowProps = {
  /** The `name` attribute for the `<input>` for selection.
If present, this structured list header row will show its selectable version of the UI. */
  selectionName?: string;
};
